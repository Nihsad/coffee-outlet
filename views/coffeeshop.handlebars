<div class="bg-light">
    <section class="container container-fluid mt-5 mb-5">
        <div class="d-flex justify-content-center">
            <div class="card" style="width: 32rem;">
                <img class="imgCoffee card-img-top" src="{{picture}}" alt="Coffeeshop Picture">
                <div class="card-body">
                    <h5 class="card-title">{{name}}</h5>
                    <p class="card-text">{{address}}</p>
                </div>
                <div class="card-body bg-secondary">
                    <a href="/api/feedbacks/addfeedback" class="card-link"><i class="fas fa-comment-dots"></i> Add
                        Feedback</a>
                    <a href="tel:{{phoneNumber}}" class="card-link"><i class="fas fa-phone-alt"></i> Call</a>
                    <a href="https://www.google.com/maps/search/?api=1&query={{encodeURIComponent address}}"
                        target="_blank" class="card-link"><i class="fas fa-map-marker-alt"></i> Directions</a>
                    <a href="{{website}}" class="card-link"><i class="fas fa-globe"></i> Website</a>
                </div>
            </div>

        </div>
        <div class="d-flex justify-content-center">
            <div class="mt-3" id="map" data-lat="{{latitude}}" data-lng="{{longitude}}" data-name="{{name}}"></div>
        </div>
        <div class="d-flex justify-content-center">
            <span>{{price_range}}</span>
        </div>
        {{#if loggedIn}}
        <div class="card mt-3 mb-3">
            <div class="card-header">
                <h5>Discover what our coffee lovers are saying! 🌟</h5>
            </div>
            <div class="card-body">
                {{#each Feedbacks as |feedback|}}
                <div class="card mb-3">
                    <div class="card-body">
                        <h2>{{feedback.description}}</h2>
                        <span class="text-muted">created on {{format_date feedback.created_on}}</span>
                        <p>By: {{feedback.user.username}}</p>
                        <div class="d-flex justify-content-end">
                            <a href="/api/feedbacks/{{id}}/edit" class="btn btn-sm btn-primary mr-2">Edit</a>
                            <form action="/api/feedbacks/{{id}}?_method=DELETE" method="POST" class="d-inline">
                                <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>
        {{/if}}

    </section>

</div>





<script src="/js/map.js"></script>